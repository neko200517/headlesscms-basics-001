{
  "name": "front",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cat .env._local > .env.local && next dev",
    "dev:prod": "cat .env._prod > .env.local && next dev",
    "build:local": "cat .env._local > .env.local && next build",
    "build:prod": "cat .env._prod > .env.local && next build",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "14.2.4",
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^8",
    "eslint-config-next": "14.2.4"
  }
}
